{
  "name": "opencode-skill-evolution",
  "version": "1.0.2",
  "description": "OpenCode plugin that learns from skill usage and suggests relevant skills automatically",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "author": {
    "name": "bigknoxy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bigknoxy/opencode-skill-evolution.git"
  },
  "bugs": {
    "url": "https://github.com/bigknoxy/opencode-skill-evolution/issues"
  },
  "homepage": "https://github.com/bigknoxy/opencode-skill-evolution#readme",
  "keywords": [
    "opencode",
    "plugin",
    "skill",
    "learning",
    "recommendations",
    "ai",
    "telemetry"
  ],
  "opencode": {
    "type": "plugin",
    "hooks": [
      "session.start",
      "tool.execute.after",
      "session.compacted"
    ]
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node",
    "dev": "bun build ./src/index.ts --outdir ./dist --target node --watch",
    "clean": "rm -rf dist",
    "rebuild": "bun run clean && bun run build",
    "test": "bun test",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.18.30",
    "@xenova/transformers": "^2.17.2",
    "glob": "^13.0.6"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  },
  "overrides": {
    "sharp": ">=0.35.0",
    "protobufjs": ">=7.6.1",
    "brace-expansion": ">=2.1.4",
    "browserslist": ">=4.29.0",
    "fast-uri": ">=3.1.6",
    "nanoid": ">=3.3.18",
    "picomatch": ">=4.0.2"
  }
}
